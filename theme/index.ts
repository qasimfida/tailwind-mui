import { createTheme } from '@mui/material/styles';

let theme = createTheme({
  spacing: 4,
  palette: {
    primary: {
      main: '#9ca3af',
    },
    secondary: {
      main: '#edf2ff',
    },
  },
});
export default theme; 